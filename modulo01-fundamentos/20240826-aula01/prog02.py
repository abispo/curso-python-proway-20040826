
# Apesar de não ser obrigatório, é uma boa prática o arquivo principal da aplicação conter a condição abaixo.
if __name__ == "__main__":

    # Tipos de dados (string, int, list, dict, ...)
    nome = "Python"     # str
    numero1 = 10        # int
    numero2 = 5.6       # float
    numero3 = 3j        # complex
    aberto = True       # boolean (True, False)
    valor = None        # None é o null do Python

    lista1 = ["Banana", "Abacaxi", "Uva"]           # Lista
    dict1 = {"nome": "Alessandro", "curso": "Java"} # Dicionário
    tupla1 = ("Maria", "João", "Carla",)            # Tupla
    set1 = {1, 2, 2, 3}                             # Set (conjunto)

    # Operadores da linguagem

    # Operadores aritméticos
    # +, -, *, /    -> Adição, Subtração, Multiplicação, Divisão
    # %, **         -> Resto da divisão, exponenciação

    # Operadores de atribuição

    # Operadores de comparação
    # ==    -> Igual a
    # !=    -> Diferente de
    # >     -> Maior que
    # <     -> Menor que
    # >=    -> Maior ou igual a
    # <=    -> Menor ou igual a

    # Operadores lógicos
    # and   -> E                    
    # or    -> OU                   
    # not   -> Não (negação)        

    # Operadores de identidade
    # is
    # is not

    # Operadores de pertencimento
    # in
    # not in

    print(((4 * 4) + 4 - 8 ** 1) >= 6 ** 1)
    print(5 * 4 + 3 / 2)

    # Python é uma linguagem case-sensitive. Ou seja, a variável nome não é a mesma que a variável Nome


    # Laços de condição (if, elif, else, case, ...)
    # Laços de repetição (for, while)
    # funções (def teste())
    # classes
    # bibliteca padrão
